services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: werte
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
